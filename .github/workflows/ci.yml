name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm run lint -- --quiet
      - run: npm run build

  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: runner-service/package-lock.json
      - run: npm --prefix runner-service ci
      - run: node --check runner-service/src/server.mjs
      - run: node --check runner-service/src/sql-runner.mjs
      - run: node --check runner-service/src/corpus-loader.mjs
