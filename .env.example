VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Keep the Supabase service-role key server-side; never expose it to Vite/browser code.
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=
# Configure Google ADC through the local gcloud CLI or an untracked environment file.
